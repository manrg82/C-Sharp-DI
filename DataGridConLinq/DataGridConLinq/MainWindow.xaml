<Window x:Class="DataGridConLinq.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataGridConLinq"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Edad Mínima" VerticalAlignment="Center" Margin="10,0"/>
            <TextBox x:Name="txtEdadMinima" Width="100" Margin="10,0"/>
            <Button x:Name="btnFiltrar" Content="Filtrar" Click="Filtrar_Click" Margin="10,0"/>
            <Button x:Name="btnMostrarTodo" Content="Mostrar Todos" Margin="10,0" Click="Mostrar_Todos_Click"/>
        </StackPanel>
        <DataGrid x:Name="dataGridPersonas" Grid.Row="1" AutoGenerateColumns="True">

        </DataGrid>

    </Grid>
</Window>
