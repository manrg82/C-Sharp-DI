<Window x:Class="ExampleMVCJson.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExampleMVCJson"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="97*" />
            <ColumnDefinition Width="63*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="btnNew" Content="NEW" Margin="20" Click="BtnNew_Click"></Button>
                <Button x:Name="btnDel" Content="DEL" Margin="20" Click="BtnDel_Click"></Button>
            </StackPanel>
            <DataGrid x:Name="dataGridUsers" AutoGenerateColumns="False" Height="350" Margin="20" SelectionMode="Single" SelectionChanged="DgvPeople_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Age" Binding="{Binding age}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10">
            <TextBlock Text="ID:" Margin="10"/>
            <TextBox x:Name="txtID" Margin="10" IsReadOnly="True"/>
            <TextBlock Text="Name:" Margin="10"/>
            <TextBox x:Name="txtName" Margin="10"/>
            <TextBlock Text="Age:" Margin="10"/>
            <TextBox x:Name="txtAge" Margin="10"/>
            <Button x:Name="btnSave" Content="SAVE" Margin="20" Click="BtnSave_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
